# `/e2e` (delphi-e2e) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dar ao plugin um comando `/e2e` que builda um projeto Delphi desktop, abre o `.exe`, opera as telas de verdade e devolve um **veredito por cenário** correlacionado com o log do app.

**Architecture:** Skill + command + entrada no RAG — **sem agente novo** (decisão 12). A execução acontece no contexto principal, passo a passo visível. O trabalho braçal de Win32 fica num harness PowerShell (`gui.ps1`) que a skill materializa em `%TEMP%` e dot-sourceia. O harness **nunca pede foco**: clique via `PostMessage`, texto via `WM_CHAR`, captura via `PrintWindow`.

**Tech Stack:** PowerShell 5.1 + P/Invoke `user32.dll`/`gdi32.dll` (via `Add-Type`), Markdown (skill/command/knowledge), Delphi FMX Win32 como alvo de teste.

**Spec:** [`docs/superpowers/specs/2026-08-09-delphi-e2e-design.md`](../specs/2026-08-09-delphi-e2e-design.md) — 16 decisões travadas. Ler §4, §5 e §6 antes de começar.

## Global Constraints

- **Windows-only.** `user32.dll` não existe fora do Windows → detectar e recusar limpo. **Nunca** apontar caminho `adb`/Android (decisão 5).
- **Nunca pedir foco.** Proibido `SetForegroundWindow`, `SetCursorPos`, `mouse_event`, `SendKeys`. Clique = `PostMessage`; texto = `WM_CHAR`; captura = `PrintWindow` flag `2`.
- **`SetProcessDPIAware()` antes de qualquer coordenada.**
- **Nomes em inglês** para skill, command e arquivos (convenção do repo, refactor `e359d92`).
- **Bilinguismo:** todo texto voltado ao usuário tem bloco `**pt-BR:**` e `**en-US:**`. Padrão pt-BR, detectado pela primeira mensagem.
- **Vereditos (decisão 13):** pt-BR `✅ PASSOU / ❌ FALHOU / ⛔ BLOQUEADO / ⏭️ PULADO` ↔ en-US `✅ PASS / ❌ FAIL / ⛔ BLOCKED / ⏭️ SKIPPED`.
- **Não reimplementar build** (decisão 3): delega à skill `delphi-build`.
- **Versão final: 3.1.0 → 3.2.0** (§6.7).
- **Sem persistência de cenários** (decisão 16). Nada é salvo em disco.
- **Parâmetro de janela** (decisão 15): default **primeiro plano**; flag para rodar ao fundo.

## Fixture de teste

Todas as tarefas do harness verificam contra um app FMX real. Fixture padrão:

- **Projeto:** `D:\Temp\Projeto GT 004\Project1.dproj` — FMX, Win32, `TForm3` com um `Button1` cujo `OnClick` faz `ShowMessage('Teste')`.
- **Build:** `call "C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat" && msbuild "D:\Temp\Projeto GT 004\Project1.dproj" /t:Build /p:Config=Debug /p:Platform=Win32`
- **Executável resultante:** `D:\Temp\Projeto GT 004\Win32\Debug\Project1.exe`

Se essa pasta não existir, qualquer app FMX Win32 com um botão visível serve. **Registrar no relatório de execução qual fixture foi usada.**

---

### Task 1: `gui.ps1` — P/Invoke e descoberta de janela

Resolve as armadilhas 1–4 da §6.4: `MainWindowHandle` inútil, várias `FMT*`, órfãs invisíveis, e app minimizado.

**Files:**
- Create: `skills/delphi-e2e/references/gui.ps1`
- Test: verificação manual contra a fixture (não há framework de teste PowerShell no repo)

**Interfaces:**
- Consumes: nada.
- Produces: `Initialize-DelphiGui`, `Get-DelphiWindow -ProcessId <int>` → `[pscustomobject]` com `Handle` (`IntPtr`), `Class` (`string`), `Visible` (`bool`), `Area` (`int`), `ClientWidth` (`int`), `ClientHeight` (`int`), `ClientOriginX` (`int`), `ClientOriginY` (`int`).

- [ ] **Step 1: Criar o arquivo com o bloco P/Invoke**

```powershell
# gui.ps1 — harness de automacao de app Delphi FMX no Windows.
# NUNCA pede foco: clique por PostMessage, texto por WM_CHAR, captura por PrintWindow.
# Materializado em %TEMP% pela skill delphi-e2e e carregado com dot-source.

Set-StrictMode -Version Latest
$ErrorActionPreference = 'Stop'

if ($env:OS -ne 'Windows_NT') {
  throw 'gui.ps1 requer Windows: user32.dll nao existe nesta plataforma.'
}

if (-not ('DelphiGui' -as [type])) {
Add-Type -Namespace '' -Name 'DelphiGui' -MemberDefinition @'
  [DllImport("user32.dll")] public static extern bool EnumWindows(EnumWindowsProc lpEnumFunc, IntPtr lParam);
  public delegate bool EnumWindowsProc(IntPtr hWnd, IntPtr lParam);
  [DllImport("user32.dll")] public static extern uint GetWindowThreadProcessId(IntPtr hWnd, out uint pid);
  [DllImport("user32.dll", CharSet=CharSet.Auto)] public static extern int GetClassName(IntPtr hWnd, System.Text.StringBuilder name, int max);
  [DllImport("user32.dll")] public static extern bool IsWindowVisible(IntPtr hWnd);
  [DllImport("user32.dll")] public static extern bool IsIconic(IntPtr hWnd);
  [DllImport("user32.dll")] public static extern bool GetWindowRect(IntPtr hWnd, out RECT r);
  [DllImport("user32.dll")] public static extern bool GetClientRect(IntPtr hWnd, out RECT r);
  [DllImport("user32.dll")] public static extern bool ClientToScreen(IntPtr hWnd, ref POINT p);
  [DllImport("user32.dll")] public static extern bool ShowWindow(IntPtr hWnd, int cmd);
  [DllImport("user32.dll")] public static extern bool SetWindowPos(IntPtr hWnd, IntPtr after, int x, int y, int cx, int cy, uint flags);
  [DllImport("user32.dll")] public static extern bool PostMessage(IntPtr hWnd, uint msg, IntPtr wParam, IntPtr lParam);
  [DllImport("user32.dll")] public static extern bool PrintWindow(IntPtr hWnd, IntPtr hdc, uint flags);
  [DllImport("user32.dll")] public static extern bool SetProcessDPIAware();
  public struct RECT { public int Left; public int Top; public int Right; public int Bottom; }
  public struct POINT { public int X; public int Y; }
'@ -UsingNamespace System.Text
}

# Constantes Win32 usadas pelo harness.
$script:SW_SHOWNOACTIVATE = 4
$script:HWND_BOTTOM       = [IntPtr]1
$script:SWP_NOSIZE        = 0x0001
$script:SWP_NOMOVE        = 0x0002
$script:SWP_NOACTIVATE    = 0x0010
$script:PW_RENDERFULLCONTENT = 2
$script:WM_MOUSEMOVE   = 0x0200
$script:WM_LBUTTONDOWN = 0x0201
$script:WM_LBUTTONUP   = 0x0202
$script:WM_CHAR        = 0x0102
$script:WM_KEYDOWN     = 0x0100
$script:WM_KEYUP       = 0x0101

function Initialize-DelphiGui {
  # Sem isto, em tela com escala != 100% todas as coordenadas saem erradas.
  [DelphiGui]::SetProcessDPIAware() | Out-Null
}
```

- [ ] **Step 2: Acrescentar a descoberta de janela**

```powershell
function Get-DelphiWindow {
  <#
    Devolve a janela REAL do form FMX.
    Armadilha 1: Process.MainWindowHandle devolve a fantasma TFMAppClass (as vezes altura 0).
    Armadilha 2: ha varias FMT* — inclusive orfas INVISIVEIS do mesmo tamanho da real.
                 Escolher a VISIVEL de maior area; so "maior area" traz a orfa e o
                 PrintWindow sai preto.
    Armadilha 3: restaurar ANTES de escolher — com o app minimizado nenhuma FMT* esta
                 visivel e a selecao cai numa orfa.
  #>
  param([Parameter(Mandatory)][int]$ProcessId)

  Restore-DelphiProcess -ProcessId $ProcessId

  $script:__found = @()
  $callback = [DelphiGui+EnumWindowsProc]{
    param([IntPtr]$h, [IntPtr]$l)
    $owner = [uint32]0
    [DelphiGui]::GetWindowThreadProcessId($h, [ref]$owner) | Out-Null
    if ($owner -eq $script:__targetPid) {
      $sb = New-Object System.Text.StringBuilder 256
      [DelphiGui]::GetClassName($h, $sb, $sb.Capacity) | Out-Null
      $cls = $sb.ToString()
      if ($cls -like 'FMT*') {
        $r = New-Object DelphiGui+RECT
        [DelphiGui]::GetWindowRect($h, [ref]$r) | Out-Null
        $script:__found += [pscustomobject]@{
          Handle  = $h
          Class   = $cls
          Visible = [DelphiGui]::IsWindowVisible($h)
          Area    = [Math]::Max(0, ($r.Right - $r.Left)) * [Math]::Max(0, ($r.Bottom - $r.Top))
        }
      }
    }
    return $true
  }
  $script:__targetPid = [uint32]$ProcessId
  [DelphiGui]::EnumWindows($callback, [IntPtr]::Zero) | Out-Null

  $win = $script:__found | Where-Object { $_.Visible -and $_.Area -gt 0 } |
         Sort-Object Area -Descending | Select-Object -First 1
  if (-not $win) {
    throw "Nenhuma janela FMT* visivel no processo $ProcessId. Janelas vistas: $($script:__found.Count)."
  }

  $cr = New-Object DelphiGui+RECT
  [DelphiGui]::GetClientRect($win.Handle, [ref]$cr) | Out-Null
  $origin = New-Object DelphiGui+POINT
  [DelphiGui]::ClientToScreen($win.Handle, [ref]$origin) | Out-Null

  [pscustomobject]@{
    Handle        = $win.Handle
    Class         = $win.Class
    Visible       = $win.Visible
    Area          = $win.Area
    ClientWidth   = $cr.Right
    ClientHeight  = $cr.Bottom
    ClientOriginX = $origin.X
    ClientOriginY = $origin.Y
  }
}

function Restore-DelphiProcess {
  <#
    Armadilha 4: quem fica iconic e a TFMAppClass; os forms so viram IsWindowVisible=False.
    Restaurar o form nao adianta — tem que ser ShowWindow(SW_SHOWNOACTIVATE) na iconic.
    SW_SHOWNOACTIVATE e o que evita roubar foco ao restaurar.
  #>
  param([Parameter(Mandatory)][int]$ProcessId)
  $script:__targetPid = [uint32]$ProcessId
  $cb = [DelphiGui+EnumWindowsProc]{
    param([IntPtr]$h, [IntPtr]$l)
    $owner = [uint32]0
    [DelphiGui]::GetWindowThreadProcessId($h, [ref]$owner) | Out-Null
    if ($owner -eq $script:__targetPid -and [DelphiGui]::IsIconic($h)) {
      [DelphiGui]::ShowWindow($h, $script:SW_SHOWNOACTIVATE) | Out-Null
    }
    return $true
  }
  [DelphiGui]::EnumWindows($cb, [IntPtr]::Zero) | Out-Null
  Start-Sleep -Milliseconds 300
}

function Get-DelphiFormWindowCount {
  # Bonus da spec: o FMX cria uma janela nativa por form, entao contar FMT* detecta
  # vazamento de form. Reportar quando a contagem crescer ao longo da bateria.
  param([Parameter(Mandatory)][int]$ProcessId)
  $script:__found = @()
  Get-DelphiWindow -ProcessId $ProcessId | Out-Null
  ($script:__found | Where-Object { $_.Visible }).Count
}
```

- [ ] **Step 3: Buildar a fixture**

```powershell
& cmd /c 'call "C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat" && msbuild "D:\Temp\Projeto GT 004\Project1.dproj" /t:Build /p:Config=Debug /p:Platform=Win32'
```

Esperado: `Build succeeded` e `D:\Temp\Projeto GT 004\Win32\Debug\Project1.exe` existindo.

- [ ] **Step 4: Verificar a descoberta**

```powershell
. "$PWD\skills\delphi-e2e\references\gui.ps1"
Initialize-DelphiGui
$p = Start-Process "D:\Temp\Projeto GT 004\Win32\Debug\Project1.exe" -PassThru
Start-Sleep -Seconds 3
$w = Get-DelphiWindow -ProcessId $p.Id
$w | Format-List
Stop-Process -Id $p.Id -Force
```

Esperado: `Class` começando com `FMT`, `Visible` `True`, `ClientWidth`/`ClientHeight` **maiores que zero**. Se `ClientHeight` vier 0, a seleção pegou a fantasma — a Step 2 está errada.

- [ ] **Step 5: Verificar o caminho do app minimizado**

Repetir a Step 4, mas minimizando a janela manualmente antes de chamar `Get-DelphiWindow`. Esperado: **mesmo resultado** — o `Restore-DelphiProcess` restaura antes de escolher.

- [ ] **Step 6: Commit**

```bash
git add skills/delphi-e2e/references/gui.ps1
git commit -m "feat(e2e): harness — P/Invoke e descoberta da janela FMX real"
```

---

### Task 2: `gui.ps1` — captura por `PrintWindow` recortada na área de cliente

Resolve a armadilha 5 da §6.4 e a regra 4 da §6.3 (capturar só a janela, nunca a tela).

**Files:**
- Modify: `skills/delphi-e2e/references/gui.ps1`

**Interfaces:**
- Consumes: `Get-DelphiWindow` (Task 1).
- Produces: `Get-DelphiShot -ProcessId <int> -Path <string>` → caminho do PNG salvo. A imagem contém **apenas a área de cliente**, então as coordenadas dela batem **1:1** com as aceitas por `Invoke-DelphiClick` (Task 3).

- [ ] **Step 1: Acrescentar a função de captura**

```powershell
function Get-DelphiShot {
  <#
    PrintWindow com PW_RENDERFULLCONTENT captura mesmo com a janela coberta e sem
    tocar no foco. Captura a janela INTEIRA (com barra de titulo); recortamos a area
    de cliente para as coordenadas da imagem baterem 1:1 com as do clique.
  #>
  param(
    [Parameter(Mandatory)][int]$ProcessId,
    [Parameter(Mandatory)][string]$Path
  )
  Add-Type -AssemblyName System.Drawing

  $w = Get-DelphiWindow -ProcessId $ProcessId
  $r = New-Object DelphiGui+RECT
  [DelphiGui]::GetWindowRect($w.Handle, [ref]$r) | Out-Null
  $fullW = $r.Right - $r.Left
  $fullH = $r.Bottom - $r.Top
  if ($fullW -le 0 -or $fullH -le 0) { throw "Janela com dimensao invalida: ${fullW}x${fullH}." }

  $bmp = New-Object System.Drawing.Bitmap $fullW, $fullH
  $gfx = [System.Drawing.Graphics]::FromImage($bmp)
  $hdc = $gfx.GetHdc()
  $ok  = [DelphiGui]::PrintWindow($w.Handle, $hdc, $script:PW_RENDERFULLCONTENT)
  $gfx.ReleaseHdc($hdc)
  $gfx.Dispose()
  if (-not $ok) { $bmp.Dispose(); throw 'PrintWindow falhou.' }

  # Recorte para a area de cliente: a origem do cliente em coordenadas de tela menos
  # a origem da janela da o deslocamento da borda/titulo.
  $offX = $w.ClientOriginX - $r.Left
  $offY = $w.ClientOriginY - $r.Top
  $rect = New-Object System.Drawing.Rectangle $offX, $offY, $w.ClientWidth, $w.ClientHeight
  $client = $bmp.Clone($rect, $bmp.PixelFormat)
  $bmp.Dispose()

  $dir = Split-Path -Parent $Path
  if ($dir -and -not (Test-Path $dir)) { New-Item -ItemType Directory -Force $dir | Out-Null }
  $client.Save($Path, [System.Drawing.Imaging.ImageFormat]::Png)
  $client.Dispose()
  $Path
}

function Test-DelphiShotIsBlank {
  # Captura preta = escolhemos uma janela orfa (armadilha 2/3). Detectar em vez de
  # devolver evidencia inutil ao relatorio.
  param([Parameter(Mandatory)][string]$Path)
  Add-Type -AssemblyName System.Drawing
  $bmp = [System.Drawing.Bitmap]::FromFile($Path)
  try {
    $step = [Math]::Max(1, [int]($bmp.Width / 20))
    foreach ($x in 0..([int]($bmp.Width / $step) - 1)) {
      foreach ($y in 0..([int]($bmp.Height / $step) - 1)) {
        $c = $bmp.GetPixel($x * $step, $y * $step)
        if ($c.R -ne 0 -or $c.G -ne 0 -or $c.B -ne 0) { return $false }
      }
    }
    return $true
  } finally { $bmp.Dispose() }
}
```

- [ ] **Step 2: Verificar a captura**

```powershell
. "$PWD\skills\delphi-e2e\references\gui.ps1"
Initialize-DelphiGui
$p = Start-Process "D:\Temp\Projeto GT 004\Win32\Debug\Project1.exe" -PassThru
Start-Sleep -Seconds 3
$shot = Get-DelphiShot -ProcessId $p.Id -Path "$env:TEMP\e2e-shot1.png"
"blank = $(Test-DelphiShotIsBlank -Path $shot)"
(Get-Item $shot).Length
Stop-Process -Id $p.Id -Force
```

Esperado: `blank = False` e tamanho do arquivo > 5 KB. `blank = True` significa que a Task 1 escolheu a janela errada.

- [ ] **Step 3: Verificar com a janela COBERTA**

Repetir a Step 2 com outra janela por cima do app. Esperado: **ainda `blank = False`** — é o que `PW_RENDERFULLCONTENT` garante e o que o modo ao fundo (Task 4) depende.

- [ ] **Step 4: Commit**

```bash
git add skills/delphi-e2e/references/gui.ps1
git commit -m "feat(e2e): harness — captura por PrintWindow recortada na area de cliente"
```

---

### Task 3: `gui.ps1` — clique e digitação sem foco

**Files:**
- Modify: `skills/delphi-e2e/references/gui.ps1`

**Interfaces:**
- Consumes: `Get-DelphiWindow` (Task 1), `Get-DelphiShot` (Task 2).
- Produces: `Invoke-DelphiClick -ProcessId <int> -X <int> -Y <int>`, `Send-DelphiText -ProcessId <int> -Text <string>`, `Send-DelphiKey -ProcessId <int> -VirtualKey <int>`. `X`/`Y` são **coordenadas de cliente**, iguais às da imagem devolvida por `Get-DelphiShot`.

- [ ] **Step 1: Acrescentar clique e texto**

```powershell
function Invoke-DelphiClick {
  <#
    PostMessage entrega direto na fila da janela: nao precisa de foco e nao move o cursor.
    Coordenadas sao de CLIENTE — as mesmas da imagem recortada por Get-DelphiShot.
  #>
  param(
    [Parameter(Mandatory)][int]$ProcessId,
    [Parameter(Mandatory)][int]$X,
    [Parameter(Mandatory)][int]$Y,
    [int]$SettleMs = 250
  )
  $w = Get-DelphiWindow -ProcessId $ProcessId
  if ($X -lt 0 -or $Y -lt 0 -or $X -ge $w.ClientWidth -or $Y -ge $w.ClientHeight) {
    throw "Coordenada ($X,$Y) fora da area de cliente ($($w.ClientWidth)x$($w.ClientHeight))."
  }
  $lParam = [IntPtr](($Y -shl 16) -bor ($X -band 0xFFFF))
  [DelphiGui]::PostMessage($w.Handle, $script:WM_MOUSEMOVE,   [IntPtr]::Zero, $lParam) | Out-Null
  [DelphiGui]::PostMessage($w.Handle, $script:WM_LBUTTONDOWN, [IntPtr]1,      $lParam) | Out-Null
  [DelphiGui]::PostMessage($w.Handle, $script:WM_LBUTTONUP,   [IntPtr]::Zero, $lParam) | Out-Null
  Start-Sleep -Milliseconds $SettleMs
}

function Send-DelphiText {
  <#
    WM_CHAR nao depende de foco de teclado e NAO sofre com acento morto de teclado ABNT
    (problema classico do SendKeys).
  #>
  param(
    [Parameter(Mandatory)][int]$ProcessId,
    [Parameter(Mandatory)][AllowEmptyString()][string]$Text,
    [int]$PerCharMs = 20
  )
  $w = Get-DelphiWindow -ProcessId $ProcessId
  foreach ($ch in $Text.ToCharArray()) {
    [DelphiGui]::PostMessage($w.Handle, $script:WM_CHAR, [IntPtr][int]$ch, [IntPtr]::Zero) | Out-Null
    Start-Sleep -Milliseconds $PerCharMs
  }
}

function Send-DelphiKey {
  # Teclas sem caractere: Esc = 27, Enter = 13, Tab = 9, Backspace = 8.
  param(
    [Parameter(Mandatory)][int]$ProcessId,
    [Parameter(Mandatory)][int]$VirtualKey,
    [int]$SettleMs = 200
  )
  $w = Get-DelphiWindow -ProcessId $ProcessId
  [DelphiGui]::PostMessage($w.Handle, $script:WM_KEYDOWN, [IntPtr]$VirtualKey, [IntPtr]::Zero) | Out-Null
  [DelphiGui]::PostMessage($w.Handle, $script:WM_KEYUP,   [IntPtr]$VirtualKey, [IntPtr]::Zero) | Out-Null
  Start-Sleep -Milliseconds $SettleMs
}
```

- [ ] **Step 2: Verificar que o clique tem efeito**

O `Button1` da fixture abre um `ShowMessage`. Capturar antes e depois; a tela **tem que mudar**.

```powershell
. "$PWD\skills\delphi-e2e\references\gui.ps1"
Initialize-DelphiGui
$p = Start-Process "D:\Temp\Projeto GT 004\Win32\Debug\Project1.exe" -PassThru
Start-Sleep -Seconds 3
$before = Get-DelphiShot -ProcessId $p.Id -Path "$env:TEMP\e2e-before.png"
# Ler a imagem, localizar o Button1 e usar o centro dele como X/Y de cliente.
Invoke-DelphiClick -ProcessId $p.Id -X <centro-x-do-botao> -Y <centro-y-do-botao>
$after = Get-DelphiShot -ProcessId $p.Id -Path "$env:TEMP\e2e-after.png"
"before=$((Get-FileHash $before).Hash)"
"after =$((Get-FileHash $after).Hash)"
Stop-Process -Id $p.Id -Force
```

Esperado: hashes **diferentes**, e a imagem `after` mostrando o diálogo `Teste`. Se forem iguais, o clique não teve efeito — conferir se as coordenadas são de cliente e não de tela.

> As coordenadas do botão saem da leitura da imagem `before`. Este é exatamente o laço que a skill executa em produção (§6.4, "Roteiro"): capturar → identificar alvo → clicar → capturar de novo para confirmar.

- [ ] **Step 3: Verificar que o cursor não foi movido**

Antes de clicar, anotar `[System.Windows.Forms.Cursor]::Position` (requer `Add-Type -AssemblyName System.Windows.Forms`); conferir depois. Esperado: **posição idêntica**.

- [ ] **Step 4: Commit**

```bash
git add skills/delphi-e2e/references/gui.ps1
git commit -m "feat(e2e): harness — clique por PostMessage e texto por WM_CHAR, sem foco"
```

---

### Task 4: `gui.ps1` — modo ao fundo (decisão 15)

**Files:**
- Modify: `skills/delphi-e2e/references/gui.ps1`

**Interfaces:**
- Consumes: `Get-DelphiWindow`, `Invoke-DelphiClick`, `Send-DelphiText`.
- Produces: `Set-DelphiWindowBottom -ProcessId <int>`; e a variável de módulo `$script:DelphiKeepBottom` (`bool`, default `$false`) que, quando `$true`, faz clique e texto reposicionarem a janela ao fim.

- [ ] **Step 1: Acrescentar o reposicionamento e ligá-lo ao clique/texto**

```powershell
# Default: primeiro plano (decisao 15). O modo ao fundo e OPT-IN e custa uma chamada
# extra por interacao — o FMX ATIVA o form ao processar o clique, mesmo vindo de
# PostMessage, entao sem isto a janela sobe sozinha.
$script:DelphiKeepBottom = $false

function Set-DelphiWindowBottom {
  param([Parameter(Mandatory)][int]$ProcessId)
  $w = Get-DelphiWindow -ProcessId $ProcessId
  $flags = $script:SWP_NOMOVE -bor $script:SWP_NOSIZE -bor $script:SWP_NOACTIVATE
  [DelphiGui]::SetWindowPos($w.Handle, $script:HWND_BOTTOM, 0, 0, 0, 0, $flags) | Out-Null
}

function Set-DelphiBackgroundMode {
  param([Parameter(Mandatory)][bool]$Enabled)
  $script:DelphiKeepBottom = $Enabled
}
```

Depois, ao final de `Invoke-DelphiClick` e de `Send-DelphiText` (após o `Start-Sleep` que já existe), acrescentar:

```powershell
  if ($script:DelphiKeepBottom) { Set-DelphiWindowBottom -ProcessId $ProcessId }
```

- [ ] **Step 2: Verificar que sem o modo a janela sobe**

```powershell
Set-DelphiBackgroundMode -Enabled $false
# clicar e observar: a janela do app vem para a frente (comportamento natural do FMX)
```

Esperado: a janela sobe. **Isto confirma que o modo ao fundo é necessário** e não paranoia.

- [ ] **Step 3: Verificar que com o modo ela permanece atrás**

```powershell
Set-DelphiBackgroundMode -Enabled $true
# colocar outra janela por cima, clicar, capturar
```

Esperado: a janela **permanece atrás**, e a captura continua válida (`Test-DelphiShotIsBlank` → `False`), porque `PrintWindow` não depende de visibilidade.

- [ ] **Step 4: Commit**

```bash
git add skills/delphi-e2e/references/gui.ps1
git commit -m "feat(e2e): harness — modo ao fundo com SWP_NOACTIVATE (decisao 15)"
```

---

### Task 5: `gui.ps1` — leitura de log com lock e delta por offset

Implementa a §6.1.

**Files:**
- Modify: `skills/delphi-e2e/references/gui.ps1`

**Interfaces:**
- Consumes: nada.
- Produces: `Get-DelphiLogOffset -Path <string>` → `[long]`; `Get-DelphiLogDelta -Path <string> -FromOffset <long>` → `[pscustomobject]` com `Text` (`string`) e `NewOffset` (`long`); `Find-DelphiLogFile -ExeDir <string> -StartedAfter <datetime>` → caminho ou `$null`.

- [ ] **Step 1: Acrescentar as funções de log**

```powershell
function Get-DelphiLogOffset {
  param([Parameter(Mandatory)][string]$Path)
  if (-not (Test-Path $Path)) { return [long]0 }
  [long](Get-Item $Path).Length
}

function Get-DelphiLogDelta {
  <#
    O app Delphi costuma manter o log ABERTO com lock de escrita (TFileStream /
    TStreamWriter com fmShareDenyWrite). Get-Content falha nesse caso e a leitura do log
    quebra em SILENCIO no meio da bateria. FileShare.ReadWrite e o que permite ler.
  #>
  param(
    [Parameter(Mandatory)][string]$Path,
    [Parameter(Mandatory)][long]$FromOffset
  )
  if (-not (Test-Path $Path)) {
    return [pscustomobject]@{ Text = ''; NewOffset = [long]0 }
  }
  $fs = [System.IO.FileStream]::new(
          $Path,
          [System.IO.FileMode]::Open,
          [System.IO.FileAccess]::Read,
          [System.IO.FileShare]::ReadWrite)
  try {
    if ($FromOffset -gt $fs.Length) { $FromOffset = 0 }   # log rotacionado/truncado
    $fs.Seek($FromOffset, [System.IO.SeekOrigin]::Begin) | Out-Null
    $reader = New-Object System.IO.StreamReader($fs)
    $text = $reader.ReadToEnd()
    [pscustomobject]@{ Text = $text; NewOffset = [long]$fs.Length }
  } finally { $fs.Dispose() }
}

function Find-DelphiLogFile {
  <#
    Ordem da spec 6.1: (a) usuario informa — tratado pela skill; (b) config.ini do
    diretorio do .exe; (c) *.log/*.txt no diretorio do .exe modificados apos o start;
    (d) nada -> a skill degrada para veredito visual E DECLARA isso.
  #>
  param(
    [Parameter(Mandatory)][string]$ExeDir,
    [Parameter(Mandatory)][datetime]$StartedAfter
  )
  $ini = Join-Path $ExeDir 'config.ini'
  if (Test-Path $ini) {
    foreach ($line in Get-Content $ini) {
      if ($line -match '^\s*(Log|LogFile|LogPath)\s*=\s*(.+?)\s*$') {
        $candidate = $Matches[2]
        if (-not [System.IO.Path]::IsPathRooted($candidate)) {
          $candidate = Join-Path $ExeDir $candidate
        }
        if (Test-Path $candidate) { return $candidate }
      }
    }
  }
  Get-ChildItem -Path $ExeDir -Include *.log, *.txt -File -ErrorAction SilentlyContinue |
    Where-Object { $_.LastWriteTime -ge $StartedAfter } |
    Sort-Object LastWriteTime -Descending |
    Select-Object -First 1 -ExpandProperty FullName
}
```

- [ ] **Step 2: Verificar a leitura sob lock**

Reproduzir o lock que quebra o `Get-Content`:

```powershell
. "$PWD\skills\delphi-e2e\references\gui.ps1"
$log = "$env:TEMP\e2e-locked.log"
'linha 1' | Set-Content $log
$locked = [System.IO.FileStream]::new($log, 'Open', 'Write', 'Read')  # fmShareDenyWrite
try {
  "Get-Content: $(try { Get-Content $log -ErrorAction Stop; 'OK' } catch { 'FALHOU (esperado)' })"
  $d = Get-DelphiLogDelta -Path $log -FromOffset 0
  "Get-DelphiLogDelta: '$($d.Text.Trim())' offset=$($d.NewOffset)"
} finally { $locked.Dispose(); Remove-Item $log -Force }
```

Esperado: `Get-Content` falha; `Get-DelphiLogDelta` devolve `linha 1`. **Se o `Get-Content` não falhar, o teste não está reproduzindo o lock e não prova nada.**

- [ ] **Step 3: Verificar o delta**

```powershell
$log = "$env:TEMP\e2e-delta.log"
'primeira' | Set-Content $log
$off = Get-DelphiLogOffset -Path $log
'segunda'  | Add-Content $log
$d = Get-DelphiLogDelta -Path $log -FromOffset $off
"delta = '$($d.Text.Trim())'"
Remove-Item $log -Force
```

Esperado: `delta = 'segunda'` — **sem** `primeira`.

- [ ] **Step 4: Commit**

```bash
git add skills/delphi-e2e/references/gui.ps1
git commit -m "feat(e2e): harness — leitura de log sob lock e delta por byte offset"
```

---

### Task 6: `SKILL.md` — protocolo, cenários, vereditos e segurança

**Files:**
- Create: `skills/delphi-e2e/SKILL.md`

**Interfaces:**
- Consumes: todas as funções do `gui.ps1` (Tasks 1–5).
- Produces: a skill auto-ativável. Gatilhos **disjuntos** de `delphi-tests` (§2, achado 6): usar "E2E", "end-to-end", "abrir o app", "testar a tela", "navegar no app" — **nunca** "teste unitário", "cobertura", "DUnitX".

- [ ] **Step 1: Escrever o frontmatter e a REGRA ZERO**

```markdown
---
name: delphi-e2e
description: >
  Executa cenarios de teste end-to-end em aplicacao Delphi desktop (FMX no Windows):
  builda, abre o .exe, opera as telas de verdade e emite um veredito por cenario,
  correlacionando com o log do app. Auto-ativa em: "/e2e", "teste E2E", "end-to-end",
  "abre o app e testa", "testar a tela", "navegar no aplicativo", "simular usuario".
  NAO e teste unitario — para DUnitX use delphi-tests.
---

# Delphi E2E

## Idioma de saida
Detecte o idioma da primeira mensagem. Padrao: pt-BR. Respeite overrides explicitos.

## REGRA ZERO — plataforma e foco

1. **Windows-only.** Fora do Windows, recusar limpo e parar:
   **pt-BR:** "O /e2e depende da API `user32.dll` do Windows e nao funciona neste sistema."
   **en-US:** "/e2e relies on the Windows `user32.dll` API and does not work on this system."
   Nao sugerir `adb` nem caminho Android — esta explicitamente fora de escopo.
2. **Nunca pedir foco.** Proibido `SetForegroundWindow`, `SetCursorPos`, `mouse_event` e
   `SendKeys`. O harness usa `PostMessage`, `WM_CHAR` e `PrintWindow`. Roubar foco no meio
   da digitacao do usuario ja causou vazamento de texto para dentro do app.
```

- [ ] **Step 2: Escrever o gate de segurança (§6.3)**

```markdown
## Gate de seguranca — para e espera

Antes do PRIMEIRO clique, apresentar e **aguardar confirmacao**:

- os cenarios que pretende executar;
- **quais gravam dados** e o que gravam;
- em que modo vai rodar (primeiro plano / ao fundo);
- o unico pedido ao usuario: **deixar a janela aberta, mesmo atras das outras — nao minimizar**.

Regras invioláveis:
1. Nunca gravar **por iniciativa propria** — explorar, capturar, sair por Cancelar/Voltar/Esc.
2. Cenario que grava so roda se o usuario pediu **e** confirmou no gate. Senao: ⏭️ PULADO.
3. Capturar **so a janela do app** — `PrintWindow` garante isso por construcao.
4. Credenciais vem do projeto local; **nunca** ecoar senha em texto claro.
5. Declarar no relatorio: modo usado e estado final (app aberto/fechado, dados alterados/intactos).
```

- [ ] **Step 3: Escrever o protocolo de execução (§5.3)**

```markdown
## Ciclo por cenario

1. Marcar o offset atual do log (`Get-DelphiLogOffset`).
2. **Reconduzir ao ponto de partida** do cenario.
3. Executar os passos (`Invoke-DelphiClick` / `Send-DelphiText` / `Send-DelphiKey`).
4. Capturar (`Get-DelphiShot`) e **ler a tela**.
5. Ler o **delta** do log (`Get-DelphiLogDelta`).
6. Emitir veredito.
7. Voltar ao estado base.

**Isolamento (decisao 14).** Tentar reconduzir pela UI (Cancelar/Voltar/Esc), conferindo pelo
screenshot. Apos **2 tentativas** sem chegar, **matar o processo e reabrir o .exe**.
Se nao conseguir reconduzir, o cenario sai **⛔ BLOQUEADO — nunca ❌ FALHOU**.
Relatorio que acusa bug onde so houve contaminacao de estado e pior que relatorio nenhum.

**Entrega != efeito.** Um PostMessage pode ser entregue e nao surtir efeito se o controle FMX
nao estiver no estado esperado. Por isso o log e lido em paralelo: sem ele, "cliquei e nada
aconteceu" vira ❌ FALHOU quando deveria ser ⛔ BLOQUEADO.

**Ritmo.** Dar tempo ao app recem-iniciado; clique cedo demais na primeira tela nao pega.
```

- [ ] **Step 4: Escrever vereditos, fronteira de build e descoberta de log**

```markdown
## Vereditos

| pt-BR | en-US | Significa |
|---|---|---|
| ✅ PASSOU | ✅ PASS | Executou e bateu com a expectativa |
| ❌ FALHOU | ❌ FAIL | Executou e divergiu — **o app esta errado** |
| ⛔ BLOQUEADO | ⛔ BLOCKED | Nao deu para executar — **nao sei se o app esta errado** |
| ⏭️ PULADO | ⏭️ SKIPPED | Grava dados e nao foi autorizado no gate |

## Fronteira com o build

Esta skill **nao compila e nao ensina msbuild**. Verificar se ha binario; se nao houver ou
estiver mais velho que o fonte, **carregar a skill `delphi-build`**.

O que fica aqui: **localizar o .exe** (conferir `DCC_ExeOutput` no `.dproj` — costuma ser
`.\bin`, nao `Win32\Debug`) e **conferir os pre-requisitos ao lado dele**: `sk4d.dll`, `.ttf`,
`.db`, `config.ini`, `fbclient.dll`. E territorio de runtime, nao de build, e e o passo mais
esquecido.

## Descoberta de log

Ordem: (a) usuario informa; (b) `config.ini` do diretorio do .exe (`Log`/`LogFile`/`LogPath`);
(c) `*.log`/`*.txt` no diretorio do .exe modificados apos o start; (d) nada.

No caso (d): **oferecer instrumentacao** (ver `references/`) e, se recusada, degradar para
veredito **visual** — declarando isso no relatorio, nunca em silencio.
```

- [ ] **Step 5: Escrever a materialização do harness**

```markdown
## Carregar o harness

O diretorio do plugin e cache sobrescrito a cada atualizacao, e rodar `.ps1` de la esbarra em
ExecutionPolicy. Portanto: **ler** `references/gui.ps1` e **escrever** em `%TEMP%`, carregando
de la.

```powershell
$dst = Join-Path $env:TEMP 'delphi-e2e-gui.ps1'
Set-Content -Path $dst -Value $conteudoDoReferences -Encoding UTF8
. $dst
Initialize-DelphiGui
```
```

- [ ] **Step 6: Verificar a ativação**

```text
/plugin marketplace remove delphi-dev
/plugin marketplace add <caminho-do-checkout>
/plugin install delphi-dev@delphi-dev
```

Numa sessão nova, escrever "quero rodar um teste end-to-end no meu app Delphi". Esperado: a skill `delphi-e2e` ativa. Depois escrever "quero cobertura de testes unitarios com DUnitX". Esperado: ativa `delphi-tests` e **não** `delphi-e2e` — os gatilhos são disjuntos.

- [ ] **Step 7: Commit**

```bash
git add skills/delphi-e2e/SKILL.md
git commit -m "feat(e2e): SKILL.md — protocolo, vereditos, gate de seguranca e fronteira de build"
```

---

### Task 7: `references/` — instrumentação (as duas ofertas da §6.2)

**Files:**
- Create: `skills/delphi-e2e/references/logging-unit.md`
- Create: `skills/delphi-e2e/references/selftest-mode.md`

**Interfaces:**
- Consumes: nada.
- Produces: dois templates que o `SKILL.md` (Task 6) oferece quando não há log. Gerados pelo agente `delphi-writer`, seguindo os padrões do plugin. **Nunca aplicar sem aceite explícito** — mexem no código do usuário.

- [ ] **Step 1: Escrever `logging-unit.md`**

Unit de logging mínima: append thread-safe, timestamp, chamada simples. **Escrever em arquivo próprio, nunca em stdout** — `Writeln` com saída redirecionada não aparece sem `Flush(Output)`, e matar o processo à força descarta o buffer (ver `knowledge/core/console-writeln-sem-flush-nao-loga.md`). Gravar **a cada linha**, não no fim: se travar, a última linha diz onde.

```pascal
unit App.Log;

interface

procedure Log(const AMensagem: string);

implementation

uses
  System.SysUtils,
  System.IOUtils,
  System.SyncObjs;

var
  FLock: TCriticalSection;

procedure Log(const AMensagem: string);
var
  LArquivo: string;
  LLinha: string;
begin
  LArquivo := TPath.Combine(ExtractFilePath(ParamStr(0)), 'app.log');
  LLinha := Format('%s  %s', [FormatDateTime('yyyy-mm-dd hh:nn:ss.zzz', Now), AMensagem]);
  FLock.Enter;
  try
    TFile.AppendAllText(LArquivo, LLinha + sLineBreak, TEncoding.UTF8);
  finally
    FLock.Leave;
  end;
end;

initialization
  FLock := TCriticalSection.Create;

finalization
  FLock.Free;

end.
```

- [ ] **Step 2: Escrever `selftest-mode.md`**

Automação de UI confere **layout**. Para **lógica** — rede, sessão, cache, parsing, concorrência — o headless é mais barato e confiável. Oferecer este caminho **antes** de propor navegação quando o cenário for de lógica.

```pascal
// No .dpr, ANTES de criar qualquer form:
// FindCmdLineSwitch REMOVE UM caractere de switch: '--selftest' chega como
// '-selftest' e NAO casa com o switch 'selftest'. Aceitar as duas formas.
if FindCmdLineSwitch('selftest', True) or FindCmdLineSwitch('-selftest', True) then
begin
  Halt(ExecutarAutoteste);   // exit code = numero de falhas
end;
```

O `ExecutarAutoteste` roda a bateria contra a API real, grava `selftest.log` ao lado do executável **a cada linha**, e devolve a contagem de falhas. **Incluir um caso de concorrência** (N requisições em paralelo): expõe bug de conexão compartilhada que teste sequencial nunca pega — exatamente o que `knowledge/core/horse-conexao-por-requisicao.md` documenta.

- [ ] **Step 3: Verificar que o `SKILL.md` referencia os dois**

```bash
grep -n "logging-unit\|selftest-mode" skills/delphi-e2e/SKILL.md
```

Esperado: as duas referências presentes. Se faltar, a Task 6 precisa ser ajustada.

- [ ] **Step 4: Commit**

```bash
git add skills/delphi-e2e/references/logging-unit.md skills/delphi-e2e/references/selftest-mode.md
git commit -m "feat(e2e): templates de instrumentacao — unit de log e modo --selftest"
```

---

### Task 8: `commands/e2e.md` — a porta explícita

**Files:**
- Create: `commands/e2e.md`

**Interfaces:**
- Consumes: a skill `delphi-e2e` (Task 6).
- Produces: `/e2e [cenários]`. **Com argumento:** roteiro dirigido (`/e2e login: senha em branco, senha errada, senha certa`). **Sem argumento:** deriva um cenário "abre sem erro" por tela do menu principal (decisão 6).

- [ ] **Step 1: Escrever o comando**

```markdown
---
description: Executa cenarios de teste end-to-end num app Delphi desktop — builda, abre o .exe, opera as telas e devolve veredito por cenario
---

## Idioma de saida
Detecte o idioma da primeira mensagem. Padrao: pt-BR. Suportados: pt-BR, en-US.

## O que este comando faz

Carregar a skill `delphi-e2e` e seguir o protocolo dela.

**Argumento:**
- **Com argumento** — cada item vira um cenario. `/e2e login: senha em branco, senha errada`
  produz dois cenarios; traduzir cada um em passos concretos **olhando a tela**, nao adivinhando.
- **Sem argumento** — derivar um cenario "abre sem erro" por tela do menu principal.

**Flags:**
- `--background` — roda com a janela ao fundo, sem interromper quem esta na maquina.
  Default: primeiro plano (decisao 15).

## Fluxo

1. Localizar o `.dproj`. Se nao houver binario ou ele estiver mais velho que o fonte,
   carregar a skill `delphi-build` e compilar ate `Build OK`.
2. Localizar o `.exe` (`DCC_ExeOutput`) e conferir os pre-requisitos ao lado dele.
3. Descobrir o log (ordem da skill). Sem log, **oferecer** instrumentacao.
4. **Gate de seguranca** — listar cenarios, quais gravam, modo de janela. **Parar e esperar.**
5. Executar cenario a cenario, com narracao passo a passo visivel.
6. Emitir o relatorio.

## Relatorio

Tabela: cenario | expectativa | resultado | evidencia. Mais: o que nao rodou e por que;
estado final; modo de janela usado; e — se a contagem de janelas `FMT*` cresceu ao longo da
bateria — o alerta de possivel vazamento de form.

**pt-BR:** `✅ PASSOU / ❌ FALHOU / ⛔ BLOQUEADO / ⏭️ PULADO`
**en-US:** `✅ PASS / ❌ FAIL / ⛔ BLOCKED / ⏭️ SKIPPED`

## Pre-requisitos
- Windows (o comando recusa limpo em outros sistemas)
- RAD Studio instalado, para o passo de build
```

- [ ] **Step 2: Verificar o comando instalado**

Reinstalar o plugin localmente e rodar `/e2e` sem argumento num projeto Delphi. Esperado: o comando aparece na lista, carrega a skill e **para no gate** antes de qualquer clique.

- [ ] **Step 3: Commit**

```bash
git add commands/e2e.md
git commit -m "feat(e2e): comando /e2e com roteiro dirigido e flag --background"
```

---

### Task 9: Entrada no RAG (`knowledge/fmx/`)

**Files:**
- Create: `knowledge/fmx/fmx-win32-automacao-sem-foco.md`
- Modify: `knowledge/fmx/INDEX.md`

**Interfaces:**
- Consumes: nada.
- Produces: conhecimento tier `canonical` embarcado no `rag.db` pela CI.

- [ ] **Step 1: Copiar o conhecimento da KB compartilhada**

```bash
cp "C:/Users/User/.claude/shared/delphi-knowledge/fmx-automacao-windows-sem-foco.md" \
   knowledge/fmx/fmx-win32-automacao-sem-foco.md
```

- [ ] **Step 2: Indexar**

Acrescentar a `knowledge/fmx/INDEX.md`, na seção de plataforma/Windows:

```markdown
- [fmx-win32-automacao-sem-foco.md](fmx-win32-automacao-sem-foco.md) — Dirigir um app FMX no Windows sem roubar foco nem mexer no cursor: `PostMessage` (clique), `WM_CHAR` (texto, imune a acento morto ABNT), `PrintWindow` flag 2 (captura mesmo coberta). Cinco armadilhas: `MainWindowHandle` devolve a fantasma `TFMAppClass`; escolher a `FMT*` **visivel** de maior area (so "maior area" traz orfa e a captura sai preta); restaurar ANTES de escolher; `SW_SHOWNOACTIVATE` para app minimizado; recortar na area de cliente para as coordenadas baterem 1:1
```

- [ ] **Step 3: Verificar que a CI vai disparar**

```bash
git status --short knowledge/
```

Esperado: arquivos sob `knowledge/fmx/` modificados — é o gatilho de `.github/workflows/build-rag.yml`.

- [ ] **Step 4: Commit**

```bash
git add knowledge/fmx/
git commit -m "kb(fmx): automacao Win32 sem foco — mecanismo do /e2e"
```

---

### Task 10: Versionamento 3.2.0 e READMEs

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`
- Modify: `commands/about.md` (2 linhas: `Version:` e `Versão:`)
- Modify: `README.md`
- Modify: `README.pt-BR.md`

**Interfaces:**
- Consumes: tudo das Tasks 1–9.
- Produces: release `v3.2.0`. **A CI lê a versão do `plugin.json`** e cria o release com esse nome — pushar sem bumpar sobrescreveria o `rag.db` da v3.1.0.

- [ ] **Step 1: Bumpar as três fontes de versão**

`.claude-plugin/plugin.json` e `.claude-plugin/marketplace.json`: `"version": "3.1.0"` → `"3.2.0"`.
`commands/about.md`: `**Version:** 3.1.0` → `3.2.0` e `**Versão:** 3.1.0` → `3.2.0`.

- [ ] **Step 2: Atualizar os dois READMEs**

Na tabela de features (`README.md` em inglês, `README.pt-BR.md` em português):

```markdown
| **`/e2e`** | Runs end-to-end scenarios against a running Delphi desktop app — builds it, opens the `.exe`, drives the screens and reports a verdict per scenario |
```

```markdown
| **`/e2e`** | Executa cenarios end-to-end num app Delphi desktop em execucao — builda, abre o `.exe`, opera as telas e devolve um veredito por cenario |
```

Na tabela de skills, nos dois arquivos:

```markdown
| `delphi-e2e` | Activated by the `/e2e` command — Windows only |
```

```markdown
| `delphi-e2e` | Ativada pelo comando `/e2e` — somente Windows |
```

> Os READMEs **não têm string de versão** — não procurar por uma.

- [ ] **Step 3: Verificar a sincronia**

```bash
grep -h '"version"' .claude-plugin/plugin.json .claude-plugin/marketplace.json
grep -h "Version:\|Versão:" commands/about.md
grep -c "/e2e" README.md README.pt-BR.md
```

Esperado: quatro ocorrências de `3.2.0` e pelo menos duas de `/e2e` em cada README.

- [ ] **Step 4: Commit e push**

```bash
git add .claude-plugin/ commands/about.md README.md README.pt-BR.md
git commit -m "chore: bump para v3.2.0 — capacidade /e2e"
git push origin master
```

- [ ] **Step 5: Confirmar o release**

```bash
gh run list --limit 1
gh release view v3.2.0 --json tagName,isDraft,assets --jq '{tag:.tagName,draft:.isDraft,assets:[.assets[]|{name,size}]}'
```

Esperado: run `success`, release `v3.2.0` não-draft **com o asset `rag.db`**. Sem o asset, `npx delphi-dev` quebra para todo mundo.

---

## Self-Review

**1. Cobertura da spec.**

| Requisito | Task |
|---|---|
| §5.1 artefatos (skill, gui.ps1, templates, command, RAG) | 1–4, 6, 7, 8, 9 |
| §5.1 materialização em `%TEMP%` | 6 (Step 5) |
| §5.1 fronteira com `delphi-build` | 6 (Step 4), 8 |
| §5.2 modelo de cenário e 4 vereditos | 6 (Step 4), 8 |
| §5.3 ciclo e isolamento (decisão 14) | 6 (Step 3) |
| §6.1 descoberta e delta de log | 5 |
| §6.2 duas ofertas de instrumentação | 7 |
| §6.3 gate e regras de segurança | 6 (Step 2) |
| §6.4 armadilhas 1–5 | 1 (1–4), 2 (5) |
| §6.4 detector de vazamento | 1 (Step 2, `Get-DelphiFormWindowCount`), 8 |
| §6.5 relatório bilíngue | 8 |
| §6.6 guarda Windows-only | 1 (Step 1), 6 (Step 1) |
| §6.7 versionamento e READMEs | 10 |
| §6.8 sem persistência (decisão 16) | — nada a fazer, e nenhuma task escreve cenário em disco |
| Decisão 15 (modo de janela) | 4, 8 (flag `--background`) |

Sem lacunas.

**2. Placeholders.** Um único marcador deliberado: `<centro-x-do-botao>` / `<centro-y-do-botao>` na Task 3 Step 2 — o valor **só existe depois de ler a imagem**, e ler a imagem para achar o alvo é exatamente o laço que a skill executa em produção. Está explicado no próprio step.

**3. Consistência de tipos.** `Get-DelphiWindow` devolve `ClientWidth`/`ClientHeight`/`ClientOriginX`/`ClientOriginY`, e é isso que a Task 2 usa no recorte e a Task 3 usa na validação de limites. `Get-DelphiLogDelta` devolve `Text`/`NewOffset`, consumidos pelo ciclo da Task 6. `$script:DelphiKeepBottom` é lido por `Invoke-DelphiClick` e `Send-DelphiText`, definidos na Task 3 e modificados na Task 4 — ordem correta.

## Riscos conhecidos

1. **Não há framework de teste PowerShell no repo.** A verificação é manual e depende de um app FMX real. Se a fixture sumir, qualquer app FMX Win32 com botão serve.
2. **`Add-Type` com `EnumWindowsProc` como delegate** é a parte mais frágil do harness em PowerShell 5.1. Se a Task 1 Step 4 falhar na compilação do tipo, a alternativa é `Add-Type -TypeDefinition` com uma classe C# completa em vez de `-MemberDefinition`.
3. **VCL é fallback declarado, não validado** (decisão 5). A descoberta por classe `FMT*` **não** serve para VCL — janelas VCL têm a classe do nome do form (`TForm1`). Se o alvo for VCL, o filtro precisa mudar, e isso **não está coberto por nenhuma task**: é limitação consciente do v1.
