# delphi-dev v2.0 — Monorepo + npx Installer

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transformar o repo em monorepo npm workspaces, migrar a extensão VS Code para `packages/vscode/`, criar o installer npx `delphi-dev` que instala tudo em um comando, e configurar CI para publicar no VS Code Marketplace.

**Architecture:** npm workspaces na raiz gerencia dois pacotes: `installer/` (publicado como `delphi-dev` no npm) e `packages/vscode/` (publicado no VS Code Marketplace). O plugin content fica na raiz. O installer detecta pré-requisitos, clona/atualiza o plugin, baixa `rag.db` da última GitHub Release, instala a extensão VS Code e registra os hooks em `~/.claude/settings.json`.

**Tech Stack:** Node.js 18+, TypeScript 5, Vitest, Commander.js, node-fetch, chalk, @types/node

---

## Mapa de arquivos

| Arquivo | Ação | Responsabilidade |
|---------|------|-----------------|
| `package.json` | Criar | Root workspace — define workspaces: installer, packages/vscode |
| `tsconfig.json` | Criar | TS base config compartilhada |
| `installer/package.json` | Criar | Pacote npm `delphi-dev`, bin: `delphi-dev` |
| `installer/tsconfig.json` | Criar | Extends root, paths do installer |
| `installer/src/index.ts` | Criar | CLI entry point (commander) |
| `installer/src/detect.ts` | Criar | Detecta node, claude CLI, vscode, git |
| `installer/src/plugin.ts` | Criar | Instala/verifica plugin no Claude Code |
| `installer/src/vscode-ext.ts` | Criar | Instala extensão VS Code |
| `installer/src/hooks.ts` | Criar | Modifica `~/.claude/settings.json` |
| `installer/src/rag.ts` | Criar | Baixa rag.db da última GitHub Release |
| `installer/src/verify.ts` | Criar | Verifica instalação completa |
| `installer/src/ui.ts` | Criar | Output colorido (chalk), spinners |
| `installer/__tests__/detect.test.ts` | Criar | Testes unitários detect |
| `installer/__tests__/hooks.test.ts` | Criar | Testes unitários hooks |
| `installer/__tests__/rag.test.ts` | Criar | Testes unitários rag download |
| `installer/__tests__/verify.test.ts` | Criar | Testes unitários verify |
| `packages/vscode/` | Mover | Conteúdo de `delphi-dev-vscode/` migrado |
| `packages/vscode/package.json` | Modificar | Versão → 2.0.0, novos comandos |
| `.claude-plugin/plugin.json` | Modificar | Versão → 2.0.0, declara hooks |
| `.github/workflows/publish-vscode.yml` | Criar | CI publica VS Code Marketplace |
| `README.md` + `README.pt-BR.md` | Modificar | Instruções `npx delphi-dev` |
| `commands/about.md` | Modificar | Versão 2.0.0 |

---

## Task 1: Root workspace setup

**Files:**
- Create: `package.json` (root)
- Create: `tsconfig.json` (root)

- [ ] **Step 1: Criar `package.json` raiz**

```json
{
  "name": "delphi-dev-monorepo",
  "private": true,
  "workspaces": [
    "installer",
    "packages/vscode",
    "scripts"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "build:installer": "npm run build --workspace=installer",
    "build:vscode": "npm run build --workspace=packages/vscode"
  },
  "engines": {
    "node": ">=18"
  }
}
```

- [ ] **Step 2: Criar `tsconfig.json` raiz**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "moduleResolution": "node",
    "esModuleInterop": true,
    "strict": true,
    "skipLibCheck": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true
  }
}
```

- [ ] **Step 3: Rodar `npm install` na raiz**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev"
npm install
```
Esperado: `node_modules/` criado na raiz com workspaces linkados.

- [ ] **Step 4: Commit**

```bash
git add package.json tsconfig.json
git commit -m "chore: init npm workspaces monorepo root"
```

---

## Task 2: Migrar extensão VS Code para `packages/vscode/`

**Files:**
- Create: `packages/vscode/` (todo conteúdo de `delphi-dev-vscode/`)

- [ ] **Step 1: Criar pasta e copiar conteúdo**

```powershell
New-Item -ItemType Directory -Force "packages\vscode"
Copy-Item -Recurse "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\src" "packages\vscode\src"
Copy-Item -Recurse "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\media" "packages\vscode\media"
Copy-Item -Recurse "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\snippets" "packages\vscode\snippets"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\package.json" "packages\vscode\package.json"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\package-lock.json" "packages\vscode\package-lock.json"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\tsconfig.json" "packages\vscode\tsconfig.json"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\esbuild.mjs" "packages\vscode\esbuild.mjs"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\README.md" "packages\vscode\README.md"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\CHANGELOG.md" "packages\vscode\CHANGELOG.md"
Copy-Item "D:\2.2 GitHub Adriano Santos\delphi-dev-vscode\LICENSE" "packages\vscode\LICENSE"
```

- [ ] **Step 2: Atualizar `packages/vscode/package.json` — bump versão e add novos comandos**

Modificar o arquivo `packages/vscode/package.json`. Alterar `"version"` para `"2.0.0"` e adicionar os seguintes itens ao array `contributes.commands`:

```json
{
  "command": "delphi-dev.build",
  "title": "Delphi Dev: Build Project",
  "icon": "$(play)"
},
{
  "command": "delphi-dev.refactor",
  "title": "Delphi Dev: Refactor Code",
  "icon": "$(wand)"
},
{
  "command": "delphi-dev.migrate",
  "title": "Delphi Dev: Migrate Legacy Code",
  "icon": "$(arrow-right)"
},
{
  "command": "delphi-dev.contributeKb",
  "title": "Delphi Dev: Contribute to Knowledge Base",
  "icon": "$(cloud-upload)"
}
```

- [ ] **Step 3: Atualizar `packages/vscode/src/commands/delphiCommands.ts` — adicionar os 4 novos comandos**

Adicionar ao arquivo existente, após a função `executeAbout`:

```typescript
async function executeBuild(): Promise<void> {
  await sendToClaudeCode('/build');
}

async function executeRefactor(): Promise<void> {
  const ctx = getActiveFileContext();
  if (ctx) {
    await sendToClaudeCode(`/refactor ${buildFileRef(ctx.filePath)}`);
  } else {
    await sendToClaudeCode('/refactor');
  }
}

async function executeMigrate(): Promise<void> {
  const ctx = getActiveFileContext();
  if (ctx) {
    await sendToClaudeCode(`/migrate --file ${buildFileRef(ctx.filePath)}`);
  } else {
    await sendToClaudeCode('/migrate');
  }
}

async function executeContributeKb(): Promise<void> {
  await sendToClaudeCode('/contribute-kb');
}
```

E adicionar ao `registerCommands`:

```typescript
vscode.commands.registerCommand(COMMANDS.BUILD, executeBuild),
vscode.commands.registerCommand(COMMANDS.REFACTOR, executeRefactor),
vscode.commands.registerCommand(COMMANDS.MIGRATE, executeMigrate),
vscode.commands.registerCommand(COMMANDS.CONTRIBUTE_KB, executeContributeKb),
```

- [ ] **Step 4: Atualizar `packages/vscode/src/constants.ts` — adicionar novas constantes**

Adicionar ao objeto `COMMANDS` existente:

```typescript
BUILD: 'delphi-dev.build',
REFACTOR: 'delphi-dev.refactor',
MIGRATE: 'delphi-dev.migrate',
CONTRIBUTE_KB: 'delphi-dev.contributeKb',
```

- [ ] **Step 5: Atualizar `packages/vscode/src/views/delphiCommandsProvider.ts` — adicionar ao tree view**

Adicionar ao array `COMMAND_ENTRIES`:

```typescript
{
  label: 'Build Project',
  description: 'Compile and validate with delphi-builder',
  command: COMMANDS.BUILD,
  icon: new vscode.ThemeIcon('play'),
},
{
  label: 'Refactor Code',
  description: 'Refactor following Pascal idioms',
  command: COMMANDS.REFACTOR,
  icon: new vscode.ThemeIcon('wand'),
},
{
  label: 'Migrate Legacy',
  description: 'Analyze and plan legacy code modernization',
  command: COMMANDS.MIGRATE,
  icon: new vscode.ThemeIcon('arrow-right'),
},
{
  label: 'Contribute Knowledge',
  description: 'Share your learnings with the community',
  command: COMMANDS.CONTRIBUTE_KB,
  icon: new vscode.ThemeIcon('cloud-upload'),
},
```

- [ ] **Step 6: Build da extensão para verificar sem erros**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\packages\vscode"
npm install
npm run build
```
Esperado: `dist/extension.js` gerado sem erros TypeScript.

- [ ] **Step 7: Commit**

```bash
git add packages/
git commit -m "feat: migrate VS Code extension to packages/vscode, add build/refactor/migrate/contribute-kb commands (v2.0.0)"
```

---

## Task 3: Criar o pacote `installer/`

**Files:**
- Create: `installer/package.json`
- Create: `installer/tsconfig.json`
- Create: `installer/src/ui.ts`

- [ ] **Step 1: Criar `installer/package.json`**

```json
{
  "name": "delphi-dev",
  "version": "2.0.0",
  "description": "Delphi expert plugin for Claude Code — installs in one command",
  "bin": {
    "delphi-dev": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": ["delphi", "claude-code", "plugin", "rad-studio", "firemonkey"],
  "author": "Adriano Santos <adrianosantospro@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/adrianosantostreina/delphi-dev"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^12.0.0",
    "ora": "^8.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
```

- [ ] **Step 2: Criar `installer/tsconfig.json`**

```json
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*"],
  "exclude": ["__tests__", "dist"]
}
```

- [ ] **Step 3: Criar `installer/src/ui.ts`**

```typescript
import chalk from 'chalk';
import ora, { Ora } from 'ora';

export function step(message: string): void {
  console.log(chalk.cyan('→'), message);
}

export function success(message: string): void {
  console.log(chalk.green('✓'), message);
}

export function warn(message: string): void {
  console.log(chalk.yellow('⚠'), message);
}

export function error(message: string): void {
  console.log(chalk.red('✗'), message);
}

export function header(title: string): void {
  console.log('');
  console.log(chalk.bold.blue('━━━ delphi-dev installer ━━━'));
  console.log(chalk.dim(title));
  console.log('');
}

export function spinner(text: string): Ora {
  return ora({ text, color: 'cyan' }).start();
}

export function summary(items: Array<{ label: string; ok: boolean; note?: string }>): void {
  console.log('');
  console.log(chalk.bold('Installation summary:'));
  for (const item of items) {
    const icon = item.ok ? chalk.green('✓') : chalk.yellow('⚠');
    const note = item.note ? chalk.dim(` — ${item.note}`) : '';
    console.log(`  ${icon} ${item.label}${note}`);
  }
  console.log('');
}
```

- [ ] **Step 4: Instalar dependências**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\installer"
npm install
```

- [ ] **Step 5: Commit**

```bash
git add installer/
git commit -m "chore: scaffold installer package with ui helpers"
```

---

## Task 4: `detect.ts` — Detectar pré-requisitos (TDD)

**Files:**
- Create: `installer/src/detect.ts`
- Create: `installer/__tests__/detect.test.ts`

- [ ] **Step 1: Escrever o teste**

Criar `installer/__tests__/detect.test.ts`:

```typescript
import { describe, it, expect, vi, afterEach } from 'vitest';
import { detectSystem, type SystemInfo } from '../src/detect';
import { execSync } from 'child_process';

vi.mock('child_process');

const mockExecSync = vi.mocked(execSync);

afterEach(() => vi.clearAllMocks());

describe('detectSystem', () => {
  it('returns hasClaudeCLI=true when claude CLI is found', () => {
    mockExecSync.mockImplementation((cmd: string) => {
      if (String(cmd).includes('claude')) return Buffer.from('1.0.0');
      throw new Error('not found');
    });
    const info: SystemInfo = detectSystem();
    expect(info.hasClaudeCLI).toBe(true);
  });

  it('returns hasClaudeCLI=false when claude CLI throws', () => {
    mockExecSync.mockImplementation(() => { throw new Error('not found'); });
    const info = detectSystem();
    expect(info.hasClaudeCLI).toBe(false);
  });

  it('returns hasVSCode=true when code CLI is found', () => {
    mockExecSync.mockImplementation((cmd: string) => {
      if (String(cmd).includes('code')) return Buffer.from('1.88.0');
      throw new Error('not found');
    });
    const info = detectSystem();
    expect(info.hasVSCode).toBe(true);
  });

  it('includes node version', () => {
    mockExecSync.mockReturnValue(Buffer.from(''));
    const info = detectSystem();
    expect(info.nodeVersion).toBe(process.version);
  });
});
```

- [ ] **Step 2: Rodar o teste para confirmar falha**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\installer"
npx vitest run __tests__/detect.test.ts
```
Esperado: FAIL — `Cannot find module '../src/detect'`

- [ ] **Step 3: Implementar `installer/src/detect.ts`**

```typescript
import { execSync } from 'child_process';

export interface SystemInfo {
  nodeVersion: string;
  hasClaudeCLI: boolean;
  hasVSCode: boolean;
  hasGit: boolean;
}

export function detectSystem(): SystemInfo {
  return {
    nodeVersion: process.version,
    hasClaudeCLI: commandExists('claude --version'),
    hasVSCode: commandExists('code --version'),
    hasGit: commandExists('git --version'),
  };
}

function commandExists(cmd: string): boolean {
  try {
    execSync(cmd, { stdio: 'ignore' });
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 4: Rodar o teste para confirmar que passa**

```bash
npx vitest run __tests__/detect.test.ts
```
Esperado: 4 testes PASS.

- [ ] **Step 5: Commit**

```bash
git add installer/src/detect.ts installer/__tests__/detect.test.ts
git commit -m "feat(installer): add system detection (TDD)"
```

---

## Task 5: `hooks.ts` — Registrar hooks no settings.json (TDD)

**Files:**
- Create: `installer/src/hooks.ts`
- Create: `installer/__tests__/hooks.test.ts`

- [ ] **Step 1: Escrever o teste**

Criar `installer/__tests__/hooks.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

vi.mock('fs');
vi.mock('os');

const mockFs = vi.mocked(fs);
const mockOs = vi.mocked(os);

beforeEach(() => {
  mockOs.homedir.mockReturnValue('/home/user');
  vi.clearAllMocks();
});

describe('registerHooks', () => {
  it('creates hooks in empty settings file', async () => {
    mockFs.existsSync.mockReturnValue(false);
    let written = '';
    mockFs.writeFileSync.mockImplementation((_p, data) => { written = String(data); });

    const { registerHooks } = await import('../src/hooks');
    registerHooks();

    const parsed = JSON.parse(written);
    expect(parsed.hooks).toHaveProperty('SubagentStop');
    expect(parsed.hooks).toHaveProperty('Stop');
    expect(parsed.hooks).toHaveProperty('UserPromptSubmit');
    expect(parsed.hooks).toHaveProperty('PostToolUse');
  });

  it('does not overwrite existing hooks', async () => {
    const existing = { hooks: { PreToolUse: [{ matcher: '*', hooks: [] }] } };
    mockFs.existsSync.mockReturnValue(true);
    mockFs.readFileSync.mockReturnValue(JSON.stringify(existing));
    let written = '';
    mockFs.writeFileSync.mockImplementation((_p, data) => { written = String(data); });

    const { registerHooks } = await import('../src/hooks');
    registerHooks();

    const parsed = JSON.parse(written);
    expect(parsed.hooks).toHaveProperty('PreToolUse');
    expect(parsed.hooks).toHaveProperty('SubagentStop');
  });

  it('removeHooks strips only delphi hooks', async () => {
    const existing = {
      hooks: {
        SubagentStop: [{ matcher: '*', hooks: [] }],
        PreToolUse: [{ matcher: '*', hooks: [] }],
      }
    };
    mockFs.existsSync.mockReturnValue(true);
    mockFs.readFileSync.mockReturnValue(JSON.stringify(existing));
    let written = '';
    mockFs.writeFileSync.mockImplementation((_p, data) => { written = String(data); });

    const { removeHooks } = await import('../src/hooks');
    removeHooks();

    const parsed = JSON.parse(written);
    expect(parsed.hooks).not.toHaveProperty('SubagentStop');
    expect(parsed.hooks).toHaveProperty('PreToolUse');
  });
});
```

- [ ] **Step 2: Rodar para confirmar falha**

```bash
npx vitest run __tests__/hooks.test.ts
```
Esperado: FAIL — `Cannot find module '../src/hooks'`

- [ ] **Step 3: Implementar `installer/src/hooks.ts`**

```typescript
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

const PLUGIN_BASE = path.join(os.homedir(), '.claude', 'plugins', 'delphi-dev');
const SETTINGS_PATH = path.join(os.homedir(), '.claude', 'settings.json');

const DELPHI_HOOK_KEYS = ['SubagentStop', 'Stop', 'UserPromptSubmit', 'PostToolUse'] as const;

function buildHooks(): Record<string, unknown[]> {
  return {
    SubagentStop: [{
      matcher: '*',
      hooks: [{ type: 'command', command: `node "${PLUGIN_BASE}/scripts/capture.js" --mode=agent` }],
    }],
    Stop: [{
      matcher: '*',
      hooks: [{ type: 'command', command: `node "${PLUGIN_BASE}/scripts/capture.js" --mode=session` }],
    }],
    UserPromptSubmit: [{
      matcher: '*',
      hooks: [{ type: 'command', command: `node "${PLUGIN_BASE}/scripts/search.js"` }],
    }],
    PostToolUse: [{
      matcher: 'Write|Edit',
      hooks: [{ type: 'command', command: `node "${PLUGIN_BASE}/hooks/fix-encoding.js"` }],
    }],
  };
}

function readSettings(): Record<string, unknown> {
  if (!fs.existsSync(SETTINGS_PATH)) return {};
  return JSON.parse(fs.readFileSync(SETTINGS_PATH, 'utf-8')) as Record<string, unknown>;
}

function writeSettings(settings: Record<string, unknown>): void {
  const dir = path.dirname(SETTINGS_PATH);
  if (!fs.existsSync(dir)) fs.mkdirSync(dir, { recursive: true });
  fs.writeFileSync(SETTINGS_PATH, JSON.stringify(settings, null, 2));
}

export function registerHooks(): void {
  const settings = readSettings();
  const existing = (settings.hooks as Record<string, unknown[]>) ?? {};
  const newHooks = buildHooks();
  const merged: Record<string, unknown[]> = { ...existing };
  for (const [event, handlers] of Object.entries(newHooks)) {
    if (!merged[event]) merged[event] = handlers;
  }
  writeSettings({ ...settings, hooks: merged });
}

export function removeHooks(): void {
  const settings = readSettings();
  const hooks = (settings.hooks as Record<string, unknown[]>) ?? {};
  for (const key of DELPHI_HOOK_KEYS) {
    delete hooks[key];
  }
  writeSettings({ ...settings, hooks });
}
```

- [ ] **Step 4: Rodar para confirmar que passa**

```bash
npx vitest run __tests__/hooks.test.ts
```
Esperado: 3 testes PASS.

- [ ] **Step 5: Commit**

```bash
git add installer/src/hooks.ts installer/__tests__/hooks.test.ts
git commit -m "feat(installer): add hooks registration in settings.json (TDD)"
```

---

## Task 6: `rag.ts` — Baixar rag.db da GitHub Release (TDD)

**Files:**
- Create: `installer/src/rag.ts`
- Create: `installer/__tests__/rag.test.ts`

- [ ] **Step 1: Escrever o teste**

Criar `installer/__tests__/rag.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { getRagDownloadUrl, type GitHubRelease } from '../src/rag';

describe('getRagDownloadUrl', () => {
  it('returns url for rag.db asset in latest release', () => {
    const release: GitHubRelease = {
      tag_name: 'v2.0.0',
      assets: [
        { name: 'rag.db', browser_download_url: 'https://github.com/releases/rag.db' },
        { name: 'other.zip', browser_download_url: 'https://github.com/releases/other.zip' },
      ],
    };
    expect(getRagDownloadUrl(release)).toBe('https://github.com/releases/rag.db');
  });

  it('returns null when no rag.db asset found', () => {
    const release: GitHubRelease = {
      tag_name: 'v2.0.0',
      assets: [{ name: 'other.zip', browser_download_url: 'https://x.com/other.zip' }],
    };
    expect(getRagDownloadUrl(release)).toBeNull();
  });

  it('returns null for empty assets', () => {
    const release: GitHubRelease = { tag_name: 'v2.0.0', assets: [] };
    expect(getRagDownloadUrl(release)).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar para confirmar falha**

```bash
npx vitest run __tests__/rag.test.ts
```
Esperado: FAIL — `Cannot find module '../src/rag'`

- [ ] **Step 3: Implementar `installer/src/rag.ts`**

```typescript
import * as fs from 'fs';
import * as path from 'path';
import * as https from 'https';

export interface GitHubAsset {
  name: string;
  browser_download_url: string;
}

export interface GitHubRelease {
  tag_name: string;
  assets: GitHubAsset[];
}

const GITHUB_API = 'https://api.github.com/repos/adrianosantostreina/delphi-dev/releases/latest';

export function getRagDownloadUrl(release: GitHubRelease): string | null {
  const asset = release.assets.find((a) => a.name === 'rag.db');
  return asset?.browser_download_url ?? null;
}

export async function fetchLatestRelease(): Promise<GitHubRelease> {
  return new Promise((resolve, reject) => {
    https.get(GITHUB_API, { headers: { 'User-Agent': 'delphi-dev-installer' } }, (res) => {
      let data = '';
      res.on('data', (chunk) => { data += chunk; });
      res.on('end', () => resolve(JSON.parse(data) as GitHubRelease));
      res.on('error', reject);
    }).on('error', reject);
  });
}

export async function downloadRagDb(url: string, destPath: string): Promise<void> {
  await fs.promises.mkdir(path.dirname(destPath), { recursive: true });
  return new Promise((resolve, reject) => {
    const file = fs.createWriteStream(destPath);
    https.get(url, { headers: { 'User-Agent': 'delphi-dev-installer' } }, (res) => {
      res.pipe(file);
      file.on('finish', () => { file.close(); resolve(); });
    }).on('error', (err) => {
      fs.unlink(destPath, () => reject(err));
    });
  });
}
```

- [ ] **Step 4: Rodar para confirmar que passa**

```bash
npx vitest run __tests__/rag.test.ts
```
Esperado: 3 testes PASS.

- [ ] **Step 5: Commit**

```bash
git add installer/src/rag.ts installer/__tests__/rag.test.ts
git commit -m "feat(installer): add GitHub Release rag.db downloader (TDD)"
```

---

## Task 7: `plugin.ts` + `vscode-ext.ts` — Instalação do plugin e extensão

**Files:**
- Create: `installer/src/plugin.ts`
- Create: `installer/src/vscode-ext.ts`

- [ ] **Step 1: Criar `installer/src/plugin.ts`**

```typescript
import { execSync } from 'child_process';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

const PLUGIN_DEST = path.join(os.homedir(), '.claude', 'plugins', 'delphi-dev');
const REPO_URL = 'https://github.com/adrianosantostreina/delphi-dev.git';

export function installPlugin(): void {
  if (fs.existsSync(PLUGIN_DEST)) {
    execSync(`git -C "${PLUGIN_DEST}" pull --ff-only`, { stdio: 'inherit' });
  } else {
    execSync(`git clone --depth=1 "${REPO_URL}" "${PLUGIN_DEST}"`, { stdio: 'inherit' });
  }
  execSync(`claude plugin install "${PLUGIN_DEST}"`, { stdio: 'inherit' });
}

export function isPluginInstalled(): boolean {
  try {
    const output = execSync('claude plugin list', { encoding: 'utf-8' });
    return output.includes('delphi-dev');
  } catch {
    return false;
  }
}
```

- [ ] **Step 2: Criar `installer/src/vscode-ext.ts`**

```typescript
import { execSync } from 'child_process';

const EXTENSION_ID = 'adrianosantos.delphi-dev-vscode';

export function installVSCodeExtension(): void {
  execSync(`code --install-extension ${EXTENSION_ID}`, { stdio: 'inherit' });
}

export function isVSCodeExtensionInstalled(): boolean {
  try {
    const output = execSync('code --list-extensions', { encoding: 'utf-8' });
    return output.toLowerCase().includes('adrianosantos.delphi-dev-vscode');
  } catch {
    return false;
  }
}

export function isVSCodeAvailable(): boolean {
  try {
    execSync('code --version', { stdio: 'ignore' });
    return true;
  } catch {
    return false;
  }
}
```

- [ ] **Step 3: Commit**

```bash
git add installer/src/plugin.ts installer/src/vscode-ext.ts
git commit -m "feat(installer): add claude plugin + vscode extension installers"
```

---

## Task 8: `verify.ts` + `index.ts` — CLI entry point

**Files:**
- Create: `installer/src/verify.ts`
- Create: `installer/src/index.ts`

- [ ] **Step 1: Criar `installer/src/verify.ts`**

```typescript
import { detectSystem } from './detect';
import { isPluginInstalled } from './plugin';
import { isVSCodeExtensionInstalled, isVSCodeAvailable } from './vscode-ext';
import * as fs from 'fs';
import * as os from 'os';
import * as path from 'path';

const RAG_PATH = path.join(os.homedir(), '.claude', 'plugins', 'delphi-dev', 'rag', 'rag.db');

export interface VerifyResult {
  claudeOk: boolean;
  pluginOk: boolean;
  ragOk: boolean;
  vscodeOk: boolean;
  hooksOk: boolean;
}

export function verifyInstallation(): VerifyResult {
  const system = detectSystem();
  const settingsPath = path.join(os.homedir(), '.claude', 'settings.json');
  let hooksOk = false;
  if (fs.existsSync(settingsPath)) {
    const settings = JSON.parse(fs.readFileSync(settingsPath, 'utf-8'));
    hooksOk = !!(settings.hooks?.SubagentStop && settings.hooks?.UserPromptSubmit);
  }
  return {
    claudeOk: system.hasClaudeCLI,
    pluginOk: isPluginInstalled(),
    ragOk: fs.existsSync(RAG_PATH),
    vscodeOk: !isVSCodeAvailable() || isVSCodeExtensionInstalled(),
    hooksOk,
  };
}
```

- [ ] **Step 2: Criar `installer/src/index.ts`**

```typescript
#!/usr/bin/env node
import { Command } from 'commander';
import { detectSystem } from './detect';
import { installPlugin, isPluginInstalled } from './plugin';
import { installVSCodeExtension, isVSCodeAvailable } from './vscode-ext';
import { registerHooks, removeHooks } from './hooks';
import { fetchLatestRelease, getRagDownloadUrl, downloadRagDb } from './rag';
import { verifyInstallation } from './verify';
import { header, step, success, warn, error, spinner, summary } from './ui';
import * as os from 'os';
import * as path from 'path';

const RAG_DEST = path.join(os.homedir(), '.claude', 'plugins', 'delphi-dev', 'rag', 'rag.db');

const program = new Command();
program.name('delphi-dev').description('delphi-dev Claude Code plugin installer').version('2.0.0');

program
  .command('install', { isDefault: true })
  .description('Install delphi-dev plugin, VS Code extension, and RAG database')
  .action(async () => {
    header('Installing delphi-dev v2.0');

    const sys = detectSystem();
    if (!sys.hasClaudeCLI) {
      error('Claude Code CLI not found. Install from https://claude.ai/code');
      process.exit(1);
    }
    if (!sys.hasGit) {
      error('git not found. Required for plugin installation.');
      process.exit(1);
    }

    step('Installing Claude Code plugin...');
    installPlugin();
    success('Plugin installed');

    step('Registering hooks in ~/.claude/settings.json...');
    registerHooks();
    success('Hooks registered');

    const s = spinner('Downloading RAG knowledge base...');
    try {
      const release = await fetchLatestRelease();
      const url = getRagDownloadUrl(release);
      if (url) {
        await downloadRagDb(url, RAG_DEST);
        s.succeed(`RAG database downloaded (${release.tag_name})`);
      } else {
        s.warn('RAG database not found in latest release — skipping');
      }
    } catch {
      s.warn('Could not download RAG database — run "npx delphi-dev sync-kb" later');
    }

    if (isVSCodeAvailable()) {
      step('Installing VS Code extension...');
      try {
        installVSCodeExtension();
        success('VS Code extension installed');
      } catch {
        warn('VS Code extension install failed — install manually: adrianosantos.delphi-dev-vscode');
      }
    } else {
      warn('VS Code not detected — skipping extension install');
    }

    const result = verifyInstallation();
    summary([
      { label: 'Claude Code CLI', ok: result.claudeOk },
      { label: 'delphi-dev plugin active', ok: result.pluginOk },
      { label: 'RAG database ready', ok: result.ragOk },
      { label: 'VS Code extension', ok: result.vscodeOk, note: !isVSCodeAvailable() ? 'VS Code not detected' : undefined },
      { label: 'Hooks registered', ok: result.hooksOk },
    ]);

    if (!result.pluginOk) {
      error('Plugin not active. Try: claude plugin list');
      process.exit(1);
    }
  });

program
  .command('update')
  .description('Update plugin and RAG database to latest version')
  .action(async () => {
    header('Updating delphi-dev');
    step('Pulling latest plugin...');
    installPlugin();
    success('Plugin updated');
    const s = spinner('Updating RAG database...');
    try {
      const release = await fetchLatestRelease();
      const url = getRagDownloadUrl(release);
      if (url) { await downloadRagDb(url, RAG_DEST); s.succeed('RAG updated'); }
      else s.warn('No rag.db in latest release');
    } catch { s.fail('RAG update failed'); }
  });

program
  .command('sync-kb')
  .description('Download latest RAG knowledge base from GitHub Releases')
  .action(async () => {
    const s = spinner('Downloading RAG knowledge base...');
    const release = await fetchLatestRelease();
    const url = getRagDownloadUrl(release);
    if (!url) { s.fail('No rag.db asset in latest release'); process.exit(1); }
    await downloadRagDb(url, RAG_DEST);
    s.succeed(`Downloaded rag.db from ${release.tag_name}`);
  });

program
  .command('verify')
  .description('Verify delphi-dev installation status')
  .action(() => {
    const result = verifyInstallation();
    summary([
      { label: 'Claude Code CLI', ok: result.claudeOk },
      { label: 'Plugin active', ok: result.pluginOk },
      { label: 'RAG database', ok: result.ragOk },
      { label: 'VS Code extension', ok: result.vscodeOk },
      { label: 'Hooks registered', ok: result.hooksOk },
    ]);
    const allOk = Object.values(result).every(Boolean);
    process.exit(allOk ? 0 : 1);
  });

program
  .command('uninstall')
  .description('Remove delphi-dev plugin and clean up hooks')
  .action(() => {
    header('Uninstalling delphi-dev');
    step('Removing hooks...');
    removeHooks();
    success('Hooks removed');
    warn('Plugin files remain in ~/.claude/plugins/delphi-dev — remove manually if desired');
  });

program.parse();
```

- [ ] **Step 3: Adicionar shebang e marcar como executável no build**

Em `installer/package.json`, adicionar ao script `build`:
```json
"build": "tsc --project tsconfig.json && node -e \"require('fs').chmodSync('dist/index.js', '755')\""
```

- [ ] **Step 4: Build completo do installer**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\installer"
npm run build
```
Esperado: `dist/index.js` gerado e executável.

- [ ] **Step 5: Smoke test do installer**

```bash
node "d:\2.2 GitHub Adriano Santos\delphi-dev\installer\dist\index.js" verify
```
Esperado: tabela de status impressa sem erro fatal.

- [ ] **Step 6: Rodar todos os testes do installer**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\installer"
npx vitest run
```
Esperado: todos os testes PASS.

- [ ] **Step 7: Commit**

```bash
git add installer/src/verify.ts installer/src/index.ts installer/package.json
git commit -m "feat(installer): complete CLI with install/update/sync-kb/verify/uninstall commands"
```

---

## Task 9: Atualizar `plugin.json` e `marketplace.json` para v2.0.0

**Files:**
- Modify: `.claude-plugin/plugin.json`
- Modify: `.claude-plugin/marketplace.json`

- [ ] **Step 1: Atualizar `.claude-plugin/plugin.json`**

Alterar `"version"` para `"2.0.0"` e adicionar o bloco `hooks`:

```json
{
  "name": "delphi-dev",
  "description": "Delphi development expert for Claude Code. Automatically applies Delphi Style Guide, Clean Code principles, and SOLID patterns. Includes technical audit, code review, standardized code writing, project scaffolding, build validation, and RAG-powered knowledge base.",
  "version": "2.0.0",
  "author": {
    "name": "Adriano Santos",
    "email": "adrianosantospro@gmail.com"
  },
  "homepage": "https://github.com/adrianosantostreina/delphi-dev",
  "repository": "https://github.com/adrianosantostreina/delphi-dev",
  "license": "MIT",
  "keywords": [
    "delphi", "object-pascal", "clean-code", "style-guide",
    "code-review", "audit", "firemonkey", "vcl", "firedac", "rad-studio", "rag"
  ],
  "hooks": {
    "SubagentStop": [{"matcher": "*", "hooks": [{"type": "command", "command": "node \"${PLUGIN_DIR}/scripts/capture.js\" --mode=agent"}]}],
    "Stop": [{"matcher": "*", "hooks": [{"type": "command", "command": "node \"${PLUGIN_DIR}/scripts/capture.js\" --mode=session"}]}],
    "UserPromptSubmit": [{"matcher": "*", "hooks": [{"type": "command", "command": "node \"${PLUGIN_DIR}/scripts/search.js\""}]}],
    "PostToolUse": [{"matcher": "Write|Edit", "hooks": [{"type": "command", "command": "node \"${PLUGIN_DIR}/hooks/fix-encoding.js\""}]}]
  }
}
```

- [ ] **Step 2: Atualizar `.claude-plugin/marketplace.json`**

Alterar `plugins[0].version` para `"2.0.0"` e atualizar `description` para incluir "RAG-powered knowledge base".

- [ ] **Step 3: Commit**

```bash
git add .claude-plugin/
git commit -m "chore: bump plugin to v2.0.0, declare hooks in plugin.json"
```

---

## Task 10: GitHub Actions — publicar VS Code Marketplace

**Files:**
- Create: `.github/workflows/publish-vscode.yml`

- [ ] **Step 1: Criar `.github/workflows/publish-vscode.yml`**

```yaml
name: Publish VS Code Extension

on:
  push:
    paths:
      - 'packages/vscode/package.json'
    branches:
      - main

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      should_publish: ${{ steps.check.outputs.changed }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2
      - id: check
        run: |
          OLD=$(git show HEAD^:packages/vscode/package.json | node -e "process.stdin.resume();let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>console.log(JSON.parse(d).version))")
          NEW=$(node -e "console.log(require('./packages/vscode/package.json').version)")
          echo "Old: $OLD, New: $NEW"
          [ "$OLD" != "$NEW" ] && echo "changed=true" >> $GITHUB_OUTPUT || echo "changed=false" >> $GITHUB_OUTPUT

  publish:
    needs: check-version
    if: needs.check-version.outputs.should_publish == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm ci --workspace=packages/vscode
      - run: npm run build --workspace=packages/vscode
      - name: Publish to VS Code Marketplace
        run: npx vsce publish --no-dependencies
        working-directory: packages/vscode
        env:
          VSCE_PAT: ${{ secrets.VSCE_TOKEN }}
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/publish-vscode.yml
git commit -m "ci: add VS Code Marketplace auto-publish on version bump"
```

---

## Task 11: Atualizar READMEs e `commands/about.md`

**Files:**
- Modify: `README.md`
- Modify: `README.pt-BR.md`
- Modify: `commands/about.md`

- [ ] **Step 1: Atualizar seção de instalação no `README.md`**

Substituir a seção de instalação por:

```markdown
## Installation

```bash
npx delphi-dev
```

This single command:
- Installs the Claude Code plugin
- Downloads the RAG knowledge base
- Installs the VS Code extension (if VS Code is detected)
- Registers automation hooks

**Requirements:** Node.js 18+, Claude Code CLI, git

### Update

```bash
npx delphi-dev update
```

### Verify installation

```bash
npx delphi-dev verify
```
```

- [ ] **Step 2: Atualizar `README.pt-BR.md` com o mesmo conteúdo em pt-BR**

```markdown
## Instalação

```bash
npx delphi-dev
```

Este único comando:
- Instala o plugin no Claude Code
- Baixa a base de conhecimento RAG
- Instala a extensão VS Code (se detectado)
- Registra hooks de automação

**Requisitos:** Node.js 18+, Claude Code CLI, git

### Atualizar

```bash
npx delphi-dev update
```

### Verificar instalação

```bash
npx delphi-dev verify
```
```

- [ ] **Step 3: Atualizar `commands/about.md` — versão 2.0.0**

Localizar a linha com `Versão:` e alterar para `Versão: 2.0.0`. Adicionar `npx delphi-dev` como método de instalação no bloco en-US e pt-BR.

- [ ] **Step 4: Commit final da v2.0**

```bash
git add README.md README.pt-BR.md commands/about.md
git commit -m "docs: update installation instructions for v2.0 npx installer"
git tag v2.0.0
```

---

## Task 12: Smoke test end-to-end da v2.0

- [ ] **Step 1: Testar em ambiente limpo (desinstalar primeiro se necessário)**

```bash
npx delphi-dev uninstall
```

- [ ] **Step 2: Rodar instalação completa**

```bash
npx delphi-dev install
```
Esperado: saída com todos os itens `✓` exceto possivelmente RAG (ainda não há rag.db na Release).

- [ ] **Step 3: Verificar plugin no Claude Code**

```bash
claude plugin list
```
Esperado: `delphi-dev` aparece na lista.

- [ ] **Step 4: Verificar hooks no settings.json**

```powershell
cat "$env:USERPROFILE\.claude\settings.json"
```
Esperado: JSON com chaves `SubagentStop`, `Stop`, `UserPromptSubmit`, `PostToolUse`.

- [ ] **Step 5: Verificar extensão VS Code**

```bash
code --list-extensions | findstr adrianosantos
```
Esperado: `adrianosantos.delphi-dev-vscode`

- [ ] **Step 6: Testar `/about` no Claude Code**

Abrir Claude Code e digitar `/about`.
Esperado: output mostra `Versão: 2.0.0`.

- [ ] **Step 7: Publicar no npm (quando pronto)**

```bash
cd "d:\2.2 GitHub Adriano Santos\delphi-dev\installer"
npm publish
```
Esperado: pacote `delphi-dev@2.0.0` publicado no npm registry.

---

## Critério de release v2.0

- [ ] `npx delphi-dev install` funciona em máquina Windows limpa
- [ ] `npx delphi-dev verify` retorna todos os itens OK
- [ ] Plugin aparece em `claude plugin list`
- [ ] Extensão VS Code instalada e ativa
- [ ] Hooks registrados em `settings.json`
- [ ] `/about` mostra `Versão: 2.0.0`
- [ ] Todos os testes unitários do installer passam
- [ ] Tag `v2.0.0` criada no GitHub
