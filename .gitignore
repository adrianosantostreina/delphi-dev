# Environment / secrets
.env
.env.local
*.env

# Node
node_modules/
installer/node_modules/
packages/*/node_modules/
scripts/node_modules/

# Build output (dist is published to npm, not committed to git)
installer/dist/

# Vitest
installer/.vite/

# OS
.DS_Store
Thumbs.db

# Build output e lockfile — nao versionados por design.
# A CI roda `npm install` + `npm run build`; o installer faz o wiring na maquina do usuario.
scripts/dist/
hooks/dist/
package-lock.json
